# Code Style

- TypeScript strict mode, no `any`
- ES modules (`"type": "module"`)
- Named imports (no `import * as`)
- camelCase for variables/functions, PascalCase for types/CDK constructs
- kebab-case for file names
