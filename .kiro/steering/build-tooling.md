# Build & Tooling

- **tsc** for building (`tsc --build`)
- **Biome** for linting and formatting
- **Lefthook** for git hooks
- **Commitlint** for conventional commits
- **tsx** for running CDK app (`npx tsx bin/aws-infra.ts`)
- Deploy: `npm run deploy` (cdk deploy --all)
